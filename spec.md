# SpeedUp - 달리기 기록 관리 웹앱 기획서

## 1. 웹앱 목표
초, 중, 고등학교의 학생들의 달리기 기록을 입력하고 개인 기록과 순위를 통해 달리기 기록을 향상시키려는 동기를 고취하려고 함.

## 2. 핵심기능 구상
- 학생수 입력 (30 입력하면 30명 추가), 학생 명단 생성 및 추가
- 개인별 달리기 기록 입력, 입력 횟수 추가
- 개인별 평균기록, 개인별 최고기록 표시
- 순위보기(최단시간별, 평균기록별, 1,2,3위 포디움 형태로 제공
- 기록 랭킹 보기(학급 내 최고 기록 및 보유자, 월별로 보기 또는 전체 기간 보기)
- 입력 모드, 보기 모드 구분(입력 모드에서만 기록 됨)

## 3. 유저흐름 구상
- 로그인 : 사용자 로그인(아이디, 비밀번호), 회원가입이나 이메일 인증 방식은 없음. 최대한 간단한 로그인 형태
- 학교명 입력 > 학년-반 입력 > 학생 수 입력 > 학생명단 입력(엔터로 구분) > 번호 자동 부여된 학생 명단 생성(중간에 번호 바꾸면 그 뒷번호 자동 변경 가능해야 함.)
- 학생 명단 테이블에는 기본적으로 5번의 기록 입력이 가능. 기록 입력 횟수 추가 버튼 필요.
- 입력 모드 버튼 눌러 학생 기록 입력, PC에서는 가로로 긴 화면에서 입력, 모바일에서는 학생 이름 선택하면 세로로 긴 입력 화면이 나오도록 이원화. 
- 학생 기록을 모두 입력하면 순위를 보거나 기록 랭킹을 확인.

## 4. 작동규칙

### 4.1 공통 규칙
- 모든 시간 입력은 분:초.밀리초 또는 초.밀리초를 허용한다. 예: 1:12.34, 72.34, 0:59.8.
- 내부 저장 형식은 밀리초 단위 정수로 통일한다. 소수점 둘째 자리까지 입력 시 백분의초를 밀리초로 변환한다. 예: 72.34초 → 72,340ms.
- 빈 값은 기록 없음(None)으로 처리하고 계산에서 제외한다. DNF는 명시적 상태로 저장하며 평균과 순위 산정에서 제외하되, 개인 히스토리에는 표시한다.
- 모든 정렬은 안정 정렬을 사용하며, 동점자 처리 규칙을 따른다.
- 입력 모드에서만 데이터가 변하고, 보기 모드에서는 어떠한 데이터도 변경되지 않는다.

### 4.2 로그인 및 권한
- 로그인은 단순 아이디/비밀번호 입력만 지원한다. 세션은 브라우저 로컬 저장소에 유지한다.
- Stage 1에서는 단일 사용자 기준으로 동작하며 동시 로그인/다중 기기 동기화는 제공하지 않는다.

### 4.3 학급/반 생성 및 편집
- 필수 필드: 학교명, 학년, 반.
- 반 생성 시 학생 수 지정 후 이름 일괄 입력(줄바꿈 구분). 비어 있는 줄은 무시한다.
- 번호 자동 부여: 1부터 연속 번호. 중간 번호를 변경하면 이후 학생들의 번호가 자동으로 재배열된다.
- 학생 추가/삭제 시 번호를 자동 재정렬한다. 수동 번호 충돌 시 가장 가까운 다음 번호로 밀어낸다.

### 4.4 학생 명단 규칙
- 이름은 1~30자(한글/영문/공백/하이픈)만 허용한다.
- 중복 이름 허용하되, 내부 식별자는 고유 ID로 관리한다.
- 비활성화(전학·장기결석) 학생은 숨김 처리할 수 있으며, 숨김은 계산에서 제외된다.

### 4.5 기록 입력 규칙
- 기본 입력 슬롯은 5개. "입력 슬롯 추가"를 통해 1회씩 증가한다.
- 각 슬롯에는 하나의 유효 기록만 저장된다. 잘못 입력 시 해당 슬롯을 수정하거나 비우기로 되돌릴 수 있다.
- 허용 형식 외 입력은 즉시 형식 오류로 알림을 띄우고 저장하지 않는다.
- 모바일: 학생 선택 → 단일 입력 화면에서 최근 N회 기록을 위에서 아래로 입력. 데스크톱: 가로 테이블에서 동일 열(시도 횟수) 기준으로 일괄 입력을 지원한다.
- 빠른 입력을 위해 Enter는 다음 학생 동일 슬롯으로, Shift+Enter는 이전 학생 동일 슬롯으로 이동한다.

### 4.6 유효성 검사
- 시간 값이 0 또는 음수면 오류.
- 분은 0~59, 초는 0~59, 밀리초는 0~999 범위를 벗어나면 오류.
- 알파벳 입력은 DNF만 허용. 대소문자 구분 없이 DNF로 저장.

### 4.7 계산 규칙
- 개인 최고 기록(PB): 유효 기록 중 최솟값(ms).
- 개인 평균 기록(AVG): 유효 기록의 산술평균(ms), 소수점 버림 없이 반올림해 표시. 화면 표시는 분:초.백분의초까지.
- 반 평균 기록: 모든 학생의 PB 평균. 숨김 학생과 유효 기록 없는 학생은 제외.

### 4.8 순위 산정 규칙
- "최단시간 순위": 각 학생의 PB를 기준으로 오름차순 정렬.
- "평균기록 순위": 각 학생의 AVG를 기준으로 오름차순 정렬.
- 동점 처리: PB가 같으면 ① AVG → ② 최신 PB의 기록일시가 더 빠른 학생 → ③ 번호가 빠른 학생 순으로 우선.
- 포디움: 1~3위 학생을 카드로 강조. 동점으로 3위가 여러 명일 경우 3위 모두 표시하고 배치 간격만 조정.

### 4.9 랭킹/히스토리 규칙
- 학급 내 최고 기록: 전체 기간 PB 최솟값과 보유자 목록. 동일 기록이 여러 명이면 모두 표시.
- 월별 보기: 브라우저 현지 시간 기준 월 경계. 해당 월의 유효 기록만 집계해 월간 PB/AVG/상위 3위 표시.
- 기간 필터: 전체, 최근 7일, 최근 30일, 사용자 지정 기간 지원.

### 4.10 모드 전환
- 입력 모드에서 보기 모드로 전환 시 미저장 변경 사항이 있으면 저장 확인 안내를 띄운다.
- 보기 모드에서는 정렬/필터/검색만 허용한다.

### 4.11 데이터 저장/복원
- Stage 1: 모든 데이터는 브라우저 localStorage에 저장한다. 키 체계는 학교명/학년/반으로 네임스페이스를 분리한다.
- 내보내기: JSON 파일로 다운로드. 가져오기: JSON을 드래그앤드롭 또는 파일 선택으로 병합/덮어쓰기 지원.
- 가져오기 시 충돌: 동일 반이 존재하면 덮어쓰기/병합/취소 선택지를 제공한다.

### 4.12 접근성 및 반응형
- 키보드 전용 내비게이션을 제공한다. 포커스 링은 항상 표시한다.
- 입력 필드에는 라벨과 도움문구를 제공한다. 오류 상태는 텍스트와 아이콘으로 동시에 알린다.
- 반응형 브레이크포인트: 모바일(≤640px) 세로 카드형, 태블릿(641~1024px) 2열, 데스크톱(≥1025px) 테이블형.

### 4.13 에러 처리
- 저장 실패(저장 용량 초과 등): 사용자에게 원인과 해결책(내보내기 후 초기화)을 안내한다.
- 잘못된 형식: 즉시 인라인 오류와 예시 형식 표시.
- 복구: 마지막 유효 상태 자동 저장(자동 복구 스냅샷 10분 간격).

### 4.14 성능
- 입력 디바운스 120ms, 대량 입력 시 렌더 배치 적용.
- 200명 × 20회 기록까지 프레임 저하 없이 동작하도록 최적화한다.

### 4.15 보안/프라이버시
- Stage 1은 오프라인 우선. 네트워크 전송 없음.
- 내보내기 파일에는 최소 데이터(반 메타, 학생 ID/이름, 기록, 타임스탬프)만 포함한다.

### 4.16 샘플 데이터
- 첫 진입 시 샘플 반 생성 옵션 제공. 언제든 삭제 가능.

### 4.17 초기화
- "반 초기화"는 기록만 삭제하고 명단은 유지. "반 삭제"는 명단과 기록 모두 삭제.

### 4.18 QA 체크리스트(발췌)
- 다양한 형식의 시간 입력이 올바르게 파싱/표시되는가.
- 동점자 순위가 규칙대로 일관되게 산정되는가.
- 모바일/데스크톱에서 입력 흐름이 자연스러운가.
- 내보내기/가져오기가 손실 없이 동작하는가.

## 5. 콘텐츠 예시

### 5.1 로그인 및 학급 생성 예시
- 로그인 화면: 아이디 teacher01, 비밀번호 1234 입력 → 대시보드 이동.
- 학교명: 상명초등학교
- 학년/반: 3학년 2반
- 학생 수: 5명
- 학생 이름 입력:
  - 김하늘
  - 이민준
  - 박서윤
  - 정유진
  - 최지호
- 결과: 자동 번호 부여된 명단 생성 → 1번 김하늘, 2번 이민준 …

### 5.2 기록 입력 예시
입력 모드 전환 후 첫 번째 기록 입력:
- 1번 김하늘: 1:02.35
- 2번 이민준: 59.87
- 3번 박서윤: DNF
- 4번 정유진: 1:05.22
- 5번 최지호: 1:01.10

두 번째 기록 입력:
- 1번 김하늘: 1:00.12
- 2번 이민준: 58.95
- 3번 박서윤: 1:03.40
- 4번 정유진: DNF
- 5번 최지호: 1:00.50

### 5.3 계산된 결과 예시
- 김하늘: PB 1:00.12, AVG 1:01.23
- 이민준: PB 58.95, AVG 59.41
- 박서윤: PB 1:03.40, AVG 1:03.40 (DNF 제외)
- 정유진: PB 1:05.22, AVG 1:05.22 (DNF 제외)
- 최지호: PB 1:00.50, AVG 1:00.80

### 5.4 순위보기 예시
- 최단시간 순위: 1위 이민준 (58.95) 2위 최지호 (1:00.50) 3위 김하늘 (1:00.12)
- 평균기록 순위: 1위 이민준 (59.41) 2위 최지호 (1:00.80) 3위 김하늘 (1:01.23)

### 5.5 포디움 예시
- 1위: 이민준 (금색 카드)
- 2위: 최지호 (은색 카드)
- 3위: 김하늘 (동색 카드)

### 5.6 월별 랭킹 예시
2025년 9월:
- 학급 최고기록: 58.95 (이민준)
- 1위 이민준, 2위 최지호, 3위 김하늘

## 6. 디자인

### 6.1 베이스 색깔 (주 색상)
- 밝은 블루: RGB(66, 135, 245) → 학생 친화적이고 활동적인 느낌
- 미드 그린: RGB(76, 175, 80) → 건강, 성장, 활력을 상징

### 6.2 보조 색깔
- 다크 네이비: RGB(33, 33, 66) → 본문 및 제목 강조용
- 라이트 그레이: RGB(245, 245, 245) → 배경 및 구분선
- 오렌지: RGB(255, 152, 0) → 버튼, 강조 표시
- 소프트 옐로우: RGB(255, 235, 59) → 포디움 1위 강조 색상

## 7. 브랜딩

### 1. 네임 아이덴티티
- 의미: 달리기 기록을 향상시키고, 자신을 더 빠르게 성장시키는 플랫폼.
- 톤: 에너지, 도전, 속도감.
- 발음: 초등학생도 "스피드업!" 하고 쉽게 말할 수 있어 기억에 남음.

### 2. 로고 아이디어
심볼형:
- 초시계 + 화살표(UP) → 시간 단축과 성장을 직관적으로 표현.
- 발자국 궤적이 위로 올라가는 모양.

텍스트형:
- Speed는 파란색 계열(에너지·활동적), Up은 초록색/주황색 계열(성장·도전).
- "Up"에 화살표(↑)를 결합해 시각적으로 강조.

### 3. 컬러 가이드
- 베이스: 밝은 블루 RGB(66, 135, 245) → 에너지, 활동.
- 포인트: 오렌지 RGB(255, 152, 0) → 속도감, 도전.
- 서브: 미드 그린 RGB(76, 175, 80) → 성장, 성취.

### 4. 캐릭터 확장
- 귀여운 치타 캐릭터 "업치" (Speed + Up → 업치)
- 머리띠 두르고 달리는 모습.
- 랭킹이나 포디움 화면에 등장해 학생들에게 동기 부여.

### 5. 앱 아이콘 컨셉
- 원형 배경에 파란색 그라데이션.
- 중앙에 "UP↑" 글자, 속도선 효과 추가.
- 초등학생이 봐도 "빨리 달리는 앱"임을 바로 알 수 있음.

## 개발스테이지

### Stage 1: 프론트엔드 중심 개발
- 프론트엔드 먼저 만들고, 백엔드 없이도 동작하게 구현
- localStorage를 활용한 데이터 저장
- 로그인, 학급 생성, 학생 관리, 기록 입력, 순위 보기 등 모든 핵심 기능 구현
- 반응형 디자인으로 모바일/데스크톱 지원

### Stage 2: 백엔드 연동
- REST API 방식의 백엔드를 모두 구현하고 프론트엔드와 연동
- 사용자 인증, 데이터베이스 연동
- 다중 사용자 지원 및 실시간 동기화